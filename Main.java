/**
 * The {@code Main} class serves as the entry point for the Taskable application.
 * It creates a {@code Menu} instance and starts the program.
 */
public class Main {

	/**
	 * The main method that launches the application.
	 *
	 * @param args command-line arguments (not used)
	 */
	public static void main(String[] args) {
		Menu programa = new Menu();
		programa.Welcome();
	}
}

